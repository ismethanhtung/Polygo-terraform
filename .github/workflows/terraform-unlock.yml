name: Terraform Unlock State

on:
    workflow_dispatch:
        inputs:
            lock_id:
                description: "Terraform state lock ID (copy from error log)"
                required: true
                type: string

permissions:
    id-token: write
    contents: read

env:
    AWS_REGION: ap-southeast-1
    AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}

jobs:
    terraform-unlock:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: environments/dev

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Debug - Check AWS_ROLE_ARN
              run: |
                  echo "AWS_ROLE_ARN: ${{ vars.AWS_ROLE_ARN }}"
                  echo "Repository: ${{ github.repository }}"
                  echo "Ref: ${{ github.ref }}"

            - name: Setup Terraform
              uses: hashicorp/setup-terraform@v3
              with:
                  terraform_version: 1.8.5

            - name: Configure AWS Credentials
              uses: aws-actions/configure-aws-credentials@v4
              with:
                  role-to-assume: ${{ vars.AWS_ROLE_ARN }}
                  aws-region: ${{ env.AWS_REGION }}
                  role-session-name: terraform-unlock-${{ github.run_id }}
                  role-duration-seconds: 3600

            - name: Terraform Init (no lock)
              run: terraform init -lock=false

            - name: Terraform Force Unlock
              run: terraform force-unlock -force "${{ inputs.lock_id }}"
